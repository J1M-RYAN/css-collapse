version: "3.9"
services:
  db:
    image: mongo
    ports:
      - 27017:27017
    restart: unless-stopped
  web:
    build:
      context: "./"
      dockerfile: "./node.Dockerfile"
    ports:
      - "3000:3000"
